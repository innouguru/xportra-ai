# tasks/blocked — Blocked Tasks

Move task files here when they cannot proceed. Each blocked task MUST state:

- The blocker (what is missing or ambiguous).
- What was already done / verified.
- What is needed to unblock it.
