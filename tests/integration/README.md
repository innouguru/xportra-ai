# tests/integration — Component Interaction Tests

Integration tests for interactions between components (e.g., ingestion →
storage, retrieval → reasoning). Currently empty — no components exist yet.
